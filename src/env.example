# ============================================
# REQUIRED CONFIGURATION
# ============================================
YELLOWSTONE_GRPC_HTTP=your_yellowstone_grpc_endpoint_url # Yellowstone gRPC endpoint URL
YELLOWSTONE_GRPC_TOKEN=your_yellowstone_grpc_token # Yellowstone authentication token
RPC_HTTP=your_solana_rpc_endpoint # Solana RPC endpoint URL
PRIVATE_KEY=your_base58_encoded_private_key # Base58-encoded wallet secret key (min 85 chars)
BUY_AMOUNT_IN_SOL=0.001 # SOL amount per buy transaction
BUY_SLIPPAGE=700 # Buy slippage in basis points for PumpFun (700 = 7%, default: 700)

# ============================================
# TRADING STRATEGY - PRICE DROP
# ============================================
NET_BUY_THRESHOLD_FOR_PRICE_RISING_TREND_DETECT=0.6 # Min net buy SOL for rising trend (default: 0.6)
NET_BUY_THRESHOLD_TO_TRIGGER_IMMEDIATE_SELL=4.0 # Current candle net buy SOL threshold to trigger immediate sell for bought tokens (default: 4.0)

# ============================================
# TRADING STRATEGY - BIG DROP RECOVERY (HIGHEST PRIORITY)
# ============================================
BIG_DROP_NET_BUY=-7.0 # Net buy threshold for previous candle to detect big drop (default: -7.0)
BIG_DROP_RECOVERY_NET_BUY=1.0 # Net buy threshold for current candle to detect recovery (default: 1.0)


# ============================================
# SELLING STRATEGY - PRIORITIZE NORMAL SELLING BASED ON PRICE RISING PCT
# ============================================
# When price_rising_pct > min_price_rising_pct AND current_candle_net_buy > allowed_net_buy_threshold,
# prioritize inactivity selling ahead of price drop consolidation selling
# Format: "min_price_rising_pct:allowed_net_buy_threshold" (multiple pairs comma-separated)
# Example: "10.0:-1.0,15.0:-1.7,20.0:-2.0" means:
#   - If price_rising_pct > 10% AND current_candle_net_buy > -1.0 SOL → prioritize normal selling
#   - If price_rising_pct > 15% AND current_candle_net_buy > -1.7 SOL → prioritize normal selling
#   - If price_rising_pct > 20% AND current_candle_net_buy > -2.0 SOL → prioritize normal selling
# The highest matching threshold is used (most restrictive condition)
NET_BUY_THRESHOLD_TO_ALLOW_NORMAL_SELLING_BASED_ON_PRICE_RISING_PCT=10.0:-1.0,15.0:-1.7,20.0:-2.0

# ============================================
# TRADING STRATEGY - ACTIVE BUYING
# ============================================
# Active buying is always enabled - always uses estimated_net_buy (current_net_buy + BUY_AMOUNT_IN_SOL)
MUST_CONSOLIDATION_SECS_FOR_ACTIVE_BUYING=6 # Required consolidation seconds for active buying (default: 6)
# TRADING STRATEGY - OTHER
MIN_BUY_SELL_RATIO=3.5 # Min buy/sell volume ratio for current-second candle (default: 3.5)

# ============================================
# SELLING STRATEGY - RISKY TOKENS
# ============================================

# ============================================
# SELLING STRATEGY - BIG DROP AFTER RISING (TRADER PSYCHOLOGY)
# ============================================
# When a big drop candle is detected after a rising trend, skip selling
# This considers trader psychology: big drops after rising trends often trigger
# dip buying from other traders, causing price recovery

# ============================================
# ZEROSLOT CONFIGURATION
# ============================================
ZERO_SLOT_URL=your_zeroslot_url # ZeroSlot service URL
ZERO_SLOT_HEALTH=https://ny1.0slot.trade/health # ZeroSlot health check URL (default: shown)
ZERO_SLOT_TIP_VALUE=0.0025 # ZeroSlot tip value in SOL (default: 0.0025)

# ============================================
# RISK MANAGEMENT
# ============================================
IS_SIMPLIFIED_MODE=false # When true, only allow buying whitelisted tokens (default: false)
MIN_TOKEN_AGE=0 # Minimum token age in seconds - tokens below this age are considered risky (default: 0 = disabled)

# ============================================
# Risk management thresholds based on token age (uses env cache)

# ============================================
# WHALE MONITORING
# ============================================
WHALE_THRESHOLD_BUY_SOL=3.0 # Min SOL buy amount to be considered a whale (default: 3.0)
WHALE_HOLDING_PERCENTAGE_THRESHOLD=50.0 # Max % of supply a whale can hold before being flagged (default: 50.0, some checks use 70.0)

# ============================================
# TELEGRAM NOTIFICATIONS (OPTIONAL)
# ============================================
TELEGRAM_BOT_TOKEN= # Telegram bot token (optional)
TELEGRAM_CHAT_ID= # Telegram chat ID (optional)

# ============================================
# TRANSACTION CONFIGURATION
# ============================================
UNIT_PRICE=20000 # Compute unit price for priority fee (default: 20000)
UNIT_LIMIT=200000 # Compute unit limit (default: 200000)
WRAP_AMOUNT=0.1 # SOL amount to wrap when using --wrap flag (default: 0.1)

